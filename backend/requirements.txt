# Core web framework
fastapi>=0.136.1,<1.0
uvicorn[standard]>=0.46.0,<1.0
gunicorn>=26.0.0,<27.0

# Data processing
pandas>=2.3,<3.0
numpy>=2.4.5,<3.0
# scipy: Welch's t-test for cohort comparison (B19) + statistical primitives.
# Pinned to the same major as numpy to avoid the npy_intp ABI drift seen on
# 2.x bumps. Adds ~50MB but every downstream stats endpoint can use it.
scipy>=1.14,<2.0
openpyxl>=3.1.5,<4.0

# HTTP & middleware
python-multipart>=0.0.29,<1.0
httpx>=0.28.1,<1.0
python-dotenv>=1.2.2,<2.0

# Observability
sentry-sdk[fastapi]>=2.60.0,<3.0

# Result caching
duckdb>=1.5.4,<2.0
filelock>=3.29.0,<4.0

# Task queue & caching
celery[redis]>=5.6.3,<6.0
redis>=5.0,<6.0

# Monitoring (optional — used by Flower dashboard)
flower>=2.0.1,<3.0

# S4PRED neural network inference
# Note: In Docker, use CPU-only torch via --extra-index-url
# (see Dockerfile.backend for details)
torch>=2.12.0,<3.0

# Vector similarity search — Wave 2 §D (ADR-016: LanceDB embedded) + §D-fix (ADR-017: ESM-2 8M)
# - lancedb: in-process columnar vector store at ./data/lance (no daemon, no port, no Docker sidecar)
# - transformers: ESM-2 protein language model (facebook/esm2_t6_8M_UR50D, 320-dim, ~30MB disk, ~150-250MB RAM)
# - pyarrow: required by lancedb for columnar reads
# Torch is already pinned above (S4PRED also depends on it).
# sentence-transformers was the all-MiniLM embedder dep (rejected per ADR-017 — generic English LM
# produces biologically invalid embeddings for amino acid sequences).
lancedb>=0.30.2,<1.0
transformers>=4.30,<5.0
pyarrow>=24.0.0,<25.0

# Rate limiter for /api/predict/batch + /api/uniprot/execute (production hardening).
# Wave 2.8 close-out — PRODUCTION_LOCKDOWN.md §2.2.
slowapi>=0.1.9,<1.0
