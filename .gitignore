# 🔁 PYTHON
backend/.venv/
mcp_server/.venv/
pvl-cli/.venv/
pvl-py/.venv/
__pycache__/
*.pyc

# Runtime cache (prevents uvicorn --reload from watching TANGO/PSIPRED outputs)
backend/.run_cache/

# Wave 2 §D — LanceDB local Lance files (vector embedding store, ADR-016).
# Volume-mounted in Docker; in dev they live at <repo_root>/data/lance/.
data/lance/
backend/data/lance/

# Legacy runtime directories (moved to .run_cache, but keep ignored for safety)
backend/Tango/out/
backend/Tango/work/
backend/Psipred/out/
backend/Psipred/work/



# ⚠️ Removed from ignore list ↓ (So you can get them: No worries Peleg, I'll add them back later)

#  ------------------------------
# backend/Tango/
# backend/Jpred/
# backend/Psipred/
# 📂 DATA (Still ignored — good!)
# backend/Uniprot_KB/
# backend/Uniprot_KB/Output/
# backend/*.xlsx
# backend/*.csv

# 📊 OPTIONAL — keep only small or example datasets
# *.xlsx
# *.csv
# *.tsv
# !*example*.xlsx
# !*example*.csv
# !*example*.tsv
#  ------------------------------


# 🧠 MODEL WEIGHTS & TOOL BINARIES (volume-mounted, not committed)
tools/
*.pt
*.pth
*.onnx

# 📦 NODE (Frontend)
ui/.next/
ui/node_modules/
ui/dist/

# 🔐 ENVIRONMENT FILES
.env
.env.local
.env.*.local
backend/.env
ui/.env.local
!.env.example
!backend/.env.example
!ui/.env.example

# ⚙️ SYSTEM FILES
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db


# 🪵 LOGS
*.log
*.log.*

# 🧪 TEST & COVERAGE
.coverage
htmlcov/
.pytest_cache/
.ruff_cache/
*.cover
.hypothesis/

# 🎯 TERMINAL ORCHESTRATION (temporary coordination files)
# T-INSTRUCTIONS files are local terminal-specific guides; not committed.
# T-RES-INSTRUCTIONS.md is the only one that's session-priming, not work-in-progress.
T*-INSTRUCTIONS.md
T*_DISPATCH_*.md
PLAN.md
RECOVERY_PROMPT.md
TERMINAL_SETUP.md
New_Feedback/
COWORK_PROMPTS_*.md
COWORK_V*_DISPATCH.md

# 📦 PYTHON PACKAGE BUILD ARTIFACTS
*.egg-info/
build/
dist/
.pytest_cache/

# 🤖 DEVELOPMENT / AI GENERATED FILES
# Generated documentation and temporary files from AI tools
docs/**/*.generated.*
docs/**/*.tmp.*
docs/**/*.cache.*
*.ai-temp.*
*.claude-temp.*
.claude/settings.local.json
# Keep fixtures and active docs, but ignore generated content
!backend/tests/fixtures/**
!docs/active/**

# Vitest/Vite cache (was accidentally tracked — see commit history)
node_modules/.vite/

# Local-only conflict backups (preserved for human reconciliation; not for commit)
*.wave-2-5-local-backup

# Clean-push policy 2026-06-08 — local-only working files
docs/active/SESSION_LOG.md
docs/active/*_DAILY_*.md
docs/internal/*_DAILY_*.md
_drafts/
_external/
.cursor/
.vscode/
# Terminal-dispatch one-shots (T1-T6 instruction docs, paste-once prompts)
/T*-INSTRUCTIONS.md
/T*_DISPATCH_*.md
/T-PEL*.md
/T_*.md
/COWORK_*.md
/README_EXPLAINER.md
