# T2 + T3 restart prompts — paste-ready (2026-06-07)

Both terminals stopped without shipping. New prompts below — tighter, scoped to what's actually left after the recent T1 commits on PR #79.

**Current state of main + PR #79**:
- Helix KPI card added (symmetric to SSW): ✅ shipped on PR #79
- Total Peptides moved to sub-header line: ✅ shipped on PR #79
- Provider badge S4PRED hardcoded OFF: ✅ fixed on PR #79
- UniProt query form default predictor toggles ON: ✅ fixed on PR #79
- PDF report FF-Helix % renamed to "FF-Helix score": ✅ fixed on PR #79
- Author metadata (Peleg + Landau + Said MIT email): ✅ fixed on PR #79

**What's left**: T2's backend FF threshold root fix + T3's residue colour sweep + table reorder + correlation matrix + Active Thresholds rewrite + Help.tsx 4 sections + HowItWorks split.

---

## T2 prompt — backend FF threshold root fix (PASTE THIS)

```
You are T2, backend dev terminal. Working dir:
/Users/saidazaizah/Desktop/DESY/peptide_prediction

The previous T2 session stopped without opening a PR. The 5 test files in your
backend/tests/ working tree (test_ff_thresholds_*.py + test_peleg_algorithm_parity.py
+ test_tango_ssw_fragments_surfaced.py) are scaffolds — finish them and ship.

PRE-READ (mandatory, in order):
1. docs/active/PELEG_PAPER_AND_REPO_FINDINGS.md §3 (the bug definition)
2. /Users/saidazaizah/Desktop/DESY/Expanding-_the_amyloid_landscape-main/main.py
   lines 125-178 (perform_fibril_formation_prediction — the algorithm we match)
3. docs/active/FUNCTIONAL_DISPATCH_2026_06_07.md T2 section (the deliverable list)

THE BUG (recap):
- PVL computes FF-Helix and FF-SSW flags using STATIC defaults in config.py
  (DEFAULT_MU_H_CUTOFF=0.5, DEFAULT_HYDRO_CUTOFF=0.5; or PELEG_DEFAULT_*=0.388/
  0.417 as fallbacks).
- Peleg's algorithm requires dataset-derived means OVER THE POSITIVE CLASS ONLY:
    ssw_avg_H     = mean( database[SSW prediction != -1].Hydrophobicity )
    helix_avg_uH  = mean( database[helix_present].Helix_uH )
- Then:
    FF-SSW = 1   iff (SSW prediction == 1) AND (Hydrophobicity >= ssw_avg_H)
    FF-Helix = 1 iff (helix_present)         AND (Helix uH >= helix_avg_uH)
- For single-sequence uploads the means degenerate. Fall back to the existing
  PELEG_DEFAULT_HYDRO_THRESHOLD (0.417) / PELEG_DEFAULT_HELIX_UH_THRESHOLD (0.388)
  AND set `single_sequence_fallback: true` in meta.thresholds.

DELIVERABLES (single PR titled "fix(ff-thresholds): dataset-derived FF gates per
Peleg main.py:147-170 parity"):

1. LOCATE the current FF flag computation. Grep for `ffHelixFlag`, `ffSswFlag`,
   `DEFAULT_MU_H`, `DEFAULT_HYDRO`, `PELEG_DEFAULT`. Likely sites:
   backend/consensus.py, backend/services/upload_service.py,
   backend/services/predict_service.py, backend/services/normalize.py.
   Report what you find before changing anything.

2. ADD function `compute_dataset_ff_thresholds(database: pd.DataFrame) -> dict`
   in the right module. Implementation per PELEG_PAPER_AND_REPO_FINDINGS.md §3.
   Returns:
     {
       "ssw_avg_H": float,
       "helix_avg_uH": float,
       "n_ssw_positive": int,
       "n_helix_positive": int,
       "single_sequence_fallback": bool,
     }

3. REPLACE static-threshold FF gating with the dataset-derived values for batch
   (len > 1). Single-sequence keeps PELEG_DEFAULT_* with `single_sequence_fallback=true`.

4. EXTEND backend/schemas/api_models.py MetaThresholds with additive fields:
     - sswAvgHUsed:           Optional[float]
     - helixAvgUhUsed:        Optional[float]
     - nSswPositive:          Optional[int]
     - nHelixPositive:        Optional[int]
     - singleSequenceFallback: Optional[bool]
   ADDITIVE ONLY — do not rename or remove any existing field.

5. SURFACE TANGO SSW fragments. The column exists in normalize.py around
   line 307-308 (sswFragments / tangoFragments). Add `tangoSswFragments:
   Optional[List[List[int]]]` to the per-peptide response shape so T3 can
   render the third row in DualStructureTrack honestly (no more S4PRED-beta
   masquerading as TANGO SSW).

6. FINISH the 5 test files already in your working tree. They should at
   minimum:
   - test_ff_thresholds_dataset_derived.py: 50-row synthetic dataset; verify
     ssw_avg_H and helix_avg_uH equal the manual mean over positive rows
   - test_ff_thresholds_single_sequence_fallback.py: 1-row → uses Peleg
     defaults, single_sequence_fallback=true in meta
   - test_ff_thresholds_match_peleg_algorithm.py: hand-built dataset where
     you can compute by hand; assert returns match
   - test_peleg_algorithm_parity.py: parity diagnostic between our
     auxiliary.get_secondary_structure_segments and Peleg's
     get_secondary_structure_segments. Don't change behavior — just measure
     differences on known inputs. T1 will decide match-paper or
     stay-corrected after seeing diagnostic output.
   - test_tango_ssw_fragments_surfaced.py: assert per-peptide response
     carries tangoSswFragments when TANGO ran

VERIFICATION:
  cd backend
  USE_TANGO=0 USE_S4PRED=0 .venv/bin/python -m pytest tests/ -q
  All existing 606+ tests must pass. New tests must pass.
  make contract-check
  make typecheck

WHEN DONE — DO NOT STOP at "code complete":
  1. git add the changes
  2. git commit with a descriptive message
  3. git push -u origin <branch-name>
  4. gh pr create against main
  5. Report back: PR number + the parity-diagnostic output

DO NOT TOUCH:
- Frontend code (T3 owns).
- The SSW prediction threshold logic (already matches Peleg at
  backend/tango.py:1635-1661).
- The PELEG_DEFAULT_* constants in config.py — keep as documented fallbacks.

If you discover other algorithm divergences while in there, REPORT THEM — don't
fix on the same PR (scope discipline).
```

---

## T3 prompt — frontend completion (PASTE THIS)

```
You are T3, frontend dev terminal. Working dir:
/Users/saidazaizah/Desktop/DESY/peptide_prediction

The previous T3 session stopped after partially extracting
ui/src/lib/fragmentClassification.ts from SequenceTrack. No PR was opened. The
KPI redesign has now been shipped by T1 on PR #79 — you don't need to redo it.

WHAT T1 SHIPPED ON PR #79 (don't redo):
- ResultsKpis 4 symmetric cards [%Helix, %FF-Helix, %SSW, %FF-SSW]
- Total Peptides moved to sub-header
- helixPositivePercent added to DatasetStats + datasetStore
- Provider badge S4PRED hardcoded-OFF bug fixed in Results.tsx
- UniProt query form defaults TANGO + S4PRED to ON
- PDF report FF-Helix % renamed to "FF-Helix score"

YOUR REMAINING SCOPE — open as ONE PR titled "feat(ui): residue-colour sweep +
table reorder + Active Thresholds + correlation + ranking + Help 4 sections":

1. RESIDUE COLOUR SWEEP — extend the SequenceTrack pattern (already shipped via
   ui/src/lib/fragmentClassification.ts) to:
   - ui/src/components/Mol3DViewer.tsx — read fragments first, argmax fallback
   - ui/src/components/BackboneViewer.tsx — same
   - ui/src/components/AggregationHeatmap.tsx — if it draws helix/SSW REGION
     overlays on top of the TANGO curve, those regions come from fragment
     columns, NOT per-residue argmax. (The TANGO curve itself stays as-is — it's
     showing the raw aggregation score, not a classification.)
   Add a regression test per component that mirrors the SequenceTrack pattern:
   a residue ssPrediction says "C" but is inside a helix fragment must render
   helix.

2. PEPTIDE TABLE COLUMN REORDER — ui/src/components/PeptideTable.tsx:
   New column groups L→R:
     Identity        : id, sequence, sourceDb
     Classification  : Helix flag, FF-Helix flag, SSW flag, FF-SSW flag
     BioFeatures     : Length (MOVED from Identity to here), Charge,
                        Hydrophobicity, μH
     Predictor out   : TANGO agg max, S4PRED helix SCORE (raw probability)
   Helix % column: hidden by default; toggle-visible.

3. DROP FF-HELIX % BADGE — ui/src/components/PeptideTable.tsx lines ~400-403:
   remove the "FF-Helix X%" badge in the classification pills row.
   Also update ui/src/pages/PeptideDetail.tsx scatter X-axis from
   "FF-Helix % (helix content × μH threshold)" to "FF-Helix score".
   Also update ui/src/lib/ranking.ts line ~71 METRIC_LABELS.ffHelixPercent
   from "FF-Helix %" to "FF-Helix score".
   The DATA FIELD ffHelixPercent stays — this is display labels only.

4. FIBRIL-FORMATION RANKING PRESET — ui/src/lib/ranking.ts +
   ui/src/stores/thresholdStore.ts:
   Add new preset FIBRIL_FORMATION with weights:
     ffHelixFlag (binary)   : 30
     ffSswFlag (binary)     : 30
     sswPrediction (binary) : 20
     muH                    : 10
     s4predHelixScore       : 10
   Set FIBRIL_FORMATION as default for "Recommended" mode. Existing presets
   (HELIX_FOCUS, SWITCH_FOCUS, AMYLOID_FOCUS, EQUAL) stay as alternatives.

5. ACTIVE THRESHOLDS PANEL — ui/src/components/ActiveThresholdsPanel.tsx.
   WAIT for T2's PR to land first (their backend additions to meta.thresholds
   are what you consume). Once merged:
   - Read meta.thresholds.sswDiffS4predUsed and meta.thresholds.sswDiffTangoUsed
     (already exist on the response, just unused) for the SSW diff rows.
     Display the actual applied value, sub-label "auto-derived from dataset
     (mean of N rows)".
   - Read NEW T2 fields meta.thresholds.helixAvgUhUsed and
     meta.thresholds.sswAvgHUsed for the FF threshold rows. Sub-label
     "FF-Helix μH cutoff (dataset mean of N helix-positive)".
   - For meta.thresholds.singleSequenceFallback === true: show the
     PELEG_DEFAULT_* values with sub-label "single-sequence fallback".
   - Auto-derived rows: slider disabled by default, expose "Override" button
     per row.

6. CORRELATION MATRIX — ui/src/components/CorrelationMatrix.tsx:
   - Replace S4PRED helix PERCENT metric with S4PRED helix SCORE (raw mean
     probability)
   - Add TANGO helix score, TANGO beta score, TANGO aggregation max as
     correlation metrics
   - Add FF-Helix flag and FF-SSW flag as binary correlation TARGETS so
     researchers see "which features correlate with being an FF candidate"
   - Per-cell tooltip: "Pearson r = X over N peptides where both metrics are
     present"

7. HELP.tsx — ui/src/pages/Help.tsx — 4 sections VERBATIM Peleg text:
   Ordering: Helix → FF-Helix → SSW → FF-SSW (mirrors KPI card order).

   ─ Alpha-helix secondary structure ─
   Determined by s4pred predictions and threshold.

   ─ Fibril-forming alpha helix ─
   Determined by the uH threshold. If a peptide is predicted to be helical
   (as described above) and its uH is higher than the threshold uH, it
   will be predicted as a potential alpha-helical fibril-forming peptide.

   ─ Secondary structure switch ─
   Determined by Tango and/or s4pred. Peptide will be predicted as
   secondary structure switch if the difference between averaged scores
   of helicity and extended beta are lower than the maximum gap
   threshold. Meaning, for these sequences the secondary structure
   prediction tools were indecisive or predicted scores similar for both
   secondary structures.

   ─ Fibril-forming secondary structure switch ─
   Determined by the hydrophobicity threshold. If a peptide is predicted
   to be a secondary structure switch (as described above) and its
   hydrophobicity is higher than the threshold hydrophobicity, it will be
   predicted as a potential secondary structure switch fibril-forming.

   Each <section> with short class-name heading, long form as subtitle, body
   text VERBATIM, small coloured accent matching the class badge colour.

8. HOWITWORKS DIAGRAM ORDER — ui/src/components/HowItWorks.tsx:
   Current step 2 lists "TANGO + S4PRED + FF-Helix + biochem" as parallel.
   Peleg flagged FF should be downstream.
   Split step 2 into two visual sub-steps:
     2a — "Run S4PRED + TANGO + biochem" (parallel raw inputs)
     2b — "Apply dataset-derived gates to classify FF candidates"
   Tooltip on 2b can reference "dataset mean of class-positive metric".

VERIFICATION:
  cd ui && npx tsc --noEmit
  cd ui && npx vitest run
All tests pass. Update snapshots only where intentional.

Manual smoke (open http://127.0.0.1:5173):
  /peptides/P01501 — black-G case in 3D viewer + backbone viewer renders helix
                      (extension of the SequenceTrack fix)
  /results — Active Thresholds shows dataset-derived numbers post-T2 merge
  /results — Correlation matrix has TANGO metrics + FF flag targets
  /results — Ranking dropdown defaults to "Fibril-Formation"
  /results — PeptideTable column order: Length now in BioFeatures group
  /help — 4 sections with Peleg's verbatim text
  /  — HowItWorks step 2 split into 2a/2b

WHEN DONE — DO NOT STOP at "code complete":
  1. git add the changes
  2. git commit with a descriptive message
  3. git push -u origin <branch-name>
  4. gh pr create against main
  5. Report back: PR number

DO NOT TOUCH:
- ResultsKpis.tsx (already shipped on PR #79)
- datasetStore.ts helixPositivePercent / kpi calculations (already shipped on
  PR #79)
- Backend code (T2 owns)

If you discover umbrella issues Peleg didn't catch while in there, add them to
docs/active/THINGS_PELEG_DIDNT_CATCH_2026_06_07.md and FLAG don't fix on the
same PR.
```

---

## What I (T1) will do while T2 + T3 run

If you also want me to keep landing things, I can knock off the smaller items
in parallel that don't conflict with T2/T3 scope:
- Peleg v2 follow-up doc finalize (Q2/Q6 RESOLVED markers)
- Dependabot config setup (Phase O.1)
- ISSUE-024 non-standard AA notification
- ISSUE-028 QuickAnalyze TANGO tooltip
- Validation re-run skeleton

Tell me which to start.
