# Email — Alex, FINAL (Wave 2.8/2.9 lock-in + DESY handover)

To: alex.golubev@cssb-hamburg.de
Cc: peleg.ragonis-bachar@... (Technion)
Subject: PVL is production-ready — three asks from DESY side to finalize

Hi Alex,

PVL Wave 2.8 + 2.9 is shipped, tested, and live on both Hetzner (public) and `landau-webapp-dev` (DESY VM). The DESY-side deploy works end-to-end:

- 3-hop Kerberos access path verified: `ssh azaizahs@max-display.desy.de` → `kinit` → `ssh -l root landau-webapp-dev`.
- Bootstrap script `scripts/desy_vm_bootstrap.sh` is idempotent (Docker install, /opt/pvl clone, S4PRED weight download, backend build + start, perf-env validation).
- Backend + frontend + redis + Celery workers all healthy on the VM.
- Precomputed datasets (Peleg-118: 118 peptides, gold standard Staphylococcus 2023: 2,916 peptides) generated and served on `/api/precomputed/{id}` — load in < 2 seconds vs the prior 20-minute live-pipeline path.

Full deployment guide refreshed at `docs/active/DEPLOYMENT.md`.

**Three asks from DESY side to flip the public URL**
1. **DNS** — assign a DESY hostname that A-records to `131.169.4.163`. Suggestion: `pvl.cssb-hamburg.de` or `pvl.desy.de`. With the hostname I edit one line in `/opt/pvl/docker/Caddyfile`, restart the frontend container, Caddy auto-provisions Let's Encrypt on first hit. Total time once DNS resolves: 5 minutes.
2. **Firewall** — open ports 80 and 443 inbound from the public internet to `131.169.4.163`. Until that, the VM is only reachable via SSH tunnel from inside DESY.
3. **GitLab mirror** (optional) — if DESY policy requires a mirror, the one-time setup is documented at `docs/active/GITLAB_MIRROR.md`. ~30 min, GitHub remains the source of truth, GitLab is a read-only follower.

**Everything else is wired**
- Sentry: live with DSN in GitHub secrets, runbook at `docs/active/SENTRY_RUNBOOK.md`.
- CI: backend pytest + frontend vitest + Docker build + CodeQL — all required to pass for any main merge.
- Backups: stateful volumes (`pvl-runs`, `pvl-uploads`, `pvl-cache`, `pvl-lance`, `pvl-redis`) — Docker named volumes survive container rebuilds. Whole-image rebuilds wipe the precomputed artifacts (~10 sec for Peleg-118, ~10 min for gold standard); both can be regenerated by re-running the precompute script.
- Perf instrumentation: stage timers in the predict pipeline, surfaced in Sentry traces.
- Rate limiter: 30 req/min/IP on the two expensive routes (`/api/predict/batch`, `/api/uniprot/execute`).
- Secret scanning + push protection + Dependabot vulnerability alerts + automated security fixes — all live on GitHub.

**For your awareness**
- Public deploy is the Hetzner URL `http://94.130.178.182:3000` until DESY DNS lands. Hetzner is fine to keep as a fallback after migration too — costs ~€10/month.
- The DESY VM has both reference datasets pre-baked. Once you give me a hostname, I'll update `bio.tools` and `Zenodo` metadata to point at the DESY URL.

**For the paper**
- PVL is feature-complete for the v1.0 tag. Once Peleg signs off on the six open scientific questions (mailed separately to her), we cut v1.0.0, Zenodo mints a DOI, and the paper has a citable software anchor.
- Submission packets: `docs/active/PUBLICATION_PATH.md` walks every step (Zenodo → bio.tools → JOSS) with paste-ready field values.

Said


---
*Internal note — files Alex may want to skim*:
- `docs/active/DEPLOYMENT.md` — refreshed Kerberos path + daily ops
- `docs/active/HANDOFF.md` — next-developer onboarding
- `docs/active/BACKLOG.md` — Tier 0 → Tier 4 prioritized backlog
- `docs/active/GITLAB_MIRROR.md` — only if DESY wants the mirror
- `scripts/desy_vm_bootstrap.sh` — the script that runs on the VM
