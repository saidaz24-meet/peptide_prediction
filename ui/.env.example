# Example environment configuration for the UI
# Copy this file to .env.local and update the values as needed

# API Base URL - where the backend server is running
VITE_API_BASE_URL=http://127.0.0.1:8000
